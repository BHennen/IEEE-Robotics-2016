In this folder will be all the arduino sketches(.ino files) we create for the robot.

Hopefully not much will be here but major milestones(as the test code for the library functions should be part of the library).
